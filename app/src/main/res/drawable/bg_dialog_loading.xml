<!-- res/drawable/dialog_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white"/> <!-- Warna isi dialog -->
    <corners android:radius="0dp"/> <!-- Sudut melengkung -->
    <stroke
        android:width="5dp"
        android:color="@color/blue"/>
    <padding
        android:left="16dp"
        android:right="16dp"
        android:top="16dp"
        android:bottom="16dp"/>
</shape>
